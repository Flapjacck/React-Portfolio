/**
 * index.ts
 *
 * Barrel export for TopScreen component.
 * Enables clean imports: import { TopScreen } from '@/components/TopScreen'
 */

export { TopScreen } from './TopScreen';
export type { TopScreenProps } from './TopScreen';
